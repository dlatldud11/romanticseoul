<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MapBoard">
	<select id="SelectTotalCount" resultType="int">
		select count(*) as cnt from qna	
		<if test="mode == 'id'">
			where id like #{keyword}
		</if>
		<if test="mode == 'title'">
			where title like #{keyword}
		</if>
		<if test="mode == 'content'">
			where content like #{keyword}
		</if>
	</select>
	<sql id="myselect">
		SELECT qnaseq, id, regdate, content, title, checks FROM qna
	</sql>
	<select id="SelectDataList" resultType="MyBoard">
		<include refid="myselect" />
	</select>
	<insert id="InsertData" parameterType="MyBoard">
		insert into 
		qna(qnaseq, id, regdate, content, title, checks, recontent, reregdate, remark, image)		
		values(qna_seq.nextval, #{id, jdbcType=VARCHAR}, #{regdate, jdbcType=VARCHAR}, #{content, jdbcType=VARCHAR}, #{title, jdbcType=VARCHAR}, #{checks, jdbcType=VARCHAR}, #{recontent, jdbcType=VARCHAR}, #{reregdate, jdbcType=VARCHAR}, #{remark, jdbcType=VARCHAR}, #{image, jdbcType=VARCHAR})
	</insert>
</mapper>